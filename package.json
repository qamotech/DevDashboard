{
  "name": "devdashboard-static-build",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "python quality_check.py",
    "check:links": "python scan_links.py",
    "build": "npm run check && python build_site.py",
    "lhci": "lhci autorun"
  },
  "devDependencies": {
    "esbuild": "^0.18.0",
    "postcss": "^8.4.24",
    "postcss-cli": "^10.1.0",
    "cssnano": "^5.1.14",
    "@lhci/cli": "^0.15.1"
  }
}
